#!/usr/bin/python3
def element_at(my_list, idx):
    for i in my_list[i]:
        idx = i
        if idx < 0:
            return None
        elif idx > len(my_list):
            return None
        else:
            return idx
